<%= link_to avatar_for(current_user, size: Settings.user.avatar_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_profile"), current_user, class: "profile-link" %></span>
<span class="total-microposts">
  <%= pluralize current_user.microposts.size, t(".micropost") %>
</span>
